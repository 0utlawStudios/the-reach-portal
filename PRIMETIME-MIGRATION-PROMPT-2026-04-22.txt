PRIMETIME SMM PORTAL — MIGRATION PROMPT
Generated: 2026-04-22
Apply these three bug fixes that were resolved in the source codebase today.
This is a self-contained prompt. Paste it into your Primetime Claude session.
Working directory: /Users/ace/Documents/CURSOR MAIN/PRIMETIME SMM PORTAL
================================================================================

You are working on the Primetime SMM Portal (Next.js + Supabase + Google Drive).
Apply all three fixes below. Read each file before editing. Run `npm run build`
after all changes to verify zero TypeScript errors.

================================================================================
FIX 1 OF 3: Video/MP4 Upload Fails with 413 Error
================================================================================

WHY: Every file upload goes through a Next.js API route as FormData. Vercel's
default serverless body limit is 4.5 MB. Any video file over 4.5 MB is rejected
with HTTP 413 before the route handler runs. The 25 MB client-side limit in
drive-upload.ts is irrelevant — Vercel rejects it first. Videos are broken.

THE FIX: Add a two-path system to `src/lib/drive-upload.ts`:
- Files < 4 MB: existing proxy path (unchanged, works for thumbnails/images)
- Files >= 4 MB: 3-step resumable path where the file goes browser-to-Google
  directly, bypassing Vercel entirely. The /api/drive/upload route already
  creates resumable sessions — it just has no callers.

Also fix two bugs in google-drive.ts:
- Missing `supportsAllDrives=true` on the resumable session URL (causes 404 on Shared Drives)
- Missing `contentLength` parameter (Google needs this to validate upload completeness)

And add `fileSize` to the /api/drive/upload route so it can set the content length.

--- CHANGE 1A: src/lib/google-drive.ts ---

Find the `createResumableUploadSession` function signature and update it to add
the optional `contentLength` parameter:

FIND:
  export async function createResumableUploadSession(
    fileName: string,
    mimeType: string,
    parentFolderId: string
  ): Promise<{ uploadUri: string }> {

REPLACE WITH:
  export async function createResumableUploadSession(
    fileName: string,
    mimeType: string,
    parentFolderId: string,
    contentLength?: number
  ): Promise<{ uploadUri: string }> {

Then find the `driveFetch` call inside that function and update it to add
`supportsAllDrives=true` to the URL and the `X-Upload-Content-Length` header:

FIND:
    `${DRIVE_UPLOAD}/files?uploadType=resumable`,
    {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "X-Upload-Content-Type": mimeType,
      },

REPLACE WITH:
    `${DRIVE_UPLOAD}/files?uploadType=resumable&supportsAllDrives=true`,
    {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "X-Upload-Content-Type": mimeType,
        ...(contentLength !== undefined ? { "X-Upload-Content-Length": String(contentLength) } : {}),
      },

--- CHANGE 1B: src/app/api/drive/upload/route.ts ---

Add `fileSize` to the UploadRequest interface and pass it through to
`createResumableUploadSession`.

FIND:
  interface UploadRequest {
    fileName: string;
    mimeType: string;
    folder: FolderName;
    cardId?: string;
  }

REPLACE WITH:
  interface UploadRequest {
    fileName: string;
    mimeType: string;
    folder: FolderName;
    cardId?: string;
    fileSize?: number;
  }

FIND:
    const { uploadUri } = await createResumableUploadSession(
      driveFileName,
      body.mimeType,
      parentId
    );

REPLACE WITH:
    const { uploadUri } = await createResumableUploadSession(
      driveFileName,
      body.mimeType,
      parentId,
      body.fileSize,
    );

--- CHANGE 1C: src/lib/drive-upload.ts ---

Replace the entire file content with the following. This adds the resumable path
for large files while keeping the existing proxy path for small files. The public
`uploadToDrive` function signature is unchanged — all consumer components work
without modification.

NEW CONTENT FOR src/lib/drive-upload.ts:
---
/**
 * Client-side Google Drive upload — two paths based on file size.
 *
 * Small files (< 4 MB): proxy path
 *   Client → /api/drive/proxy-upload (FormData) → Google Drive → returns fileId + URL
 *   One round-trip, proven, fast for thumbnails and small images.
 *
 * Large files (>= 4 MB): resumable path (bypasses Vercel's 4.5 MB body limit)
 *   1. Client → /api/drive/upload (JSON metadata) → returns uploadUri
 *   2. Client → uploadUri (PUT file bytes directly to Google, CORS-safe)
 *   3. Client → /api/drive/finalize (JSON fileId) → sets permissions, returns URL
 *
 * The uploadUri is a pre-authenticated Google URL — the service account token
 * is baked in server-side. The browser PUTs directly without any auth header.
 * Google's resumable upload endpoints support CORS (Access-Control-Allow-Origin: *).
 *
 * Guarantees:
 * - Never returns a blob: URL
 * - 3-attempt exponential backoff on transient failures
 * - Throws on permanent failure (caller must handle)
 * - 413 is treated as permanent (not retried)
 */

export interface DriveUploadResult {
  fileId: string;
  url: string;
  mimeType?: string;
  size?: number;
}

export type ProgressCallback = (percent: number) => void;

const RETRY_DELAYS = [2000, 8000, 32000];

// Files at or above this threshold use the resumable path to avoid Vercel's 4.5 MB body limit.
const RESUMABLE_THRESHOLD = 4 * 1024 * 1024; // 4 MB

// Instagram video limit (250 MB) — largest common platform constraint.
const MAX_FILE_SIZE = 250 * 1024 * 1024; // 250 MB

async function withRetry<T>(fn: () => Promise<T>, label: string): Promise<T> {
  let lastError: Error | null = null;
  for (let attempt = 0; attempt <= RETRY_DELAYS.length; attempt++) {
    try {
      return await fn();
    } catch (err) {
      lastError = err instanceof Error ? err : new Error(String(err));
      // Permanent failures — do not retry
      if (
        lastError.message.includes("400") ||
        lastError.message.includes("401") ||
        lastError.message.includes("403") ||
        lastError.message.includes("404") ||
        lastError.message.includes("413")
      ) {
        throw lastError;
      }
      if (attempt < RETRY_DELAYS.length) {
        console.warn(`[drive-upload] ${label} attempt ${attempt + 1} failed, retrying in ${RETRY_DELAYS[attempt]}ms...`);
        await new Promise((r) => setTimeout(r, RETRY_DELAYS[attempt]));
      }
    }
  }
  throw lastError || new Error(`${label} failed after ${RETRY_DELAYS.length + 1} attempts`);
}

// ─── Proxy path (< 4 MB) ───────────────────────────────────────────────────

async function uploadViaProxy(
  file: File,
  folder: string,
  cardId: string | undefined,
  onProgress: ProgressCallback | undefined
): Promise<DriveUploadResult> {
  return withRetry(async () => {
    const formData = new FormData();
    formData.append("file", file);
    formData.append("folder", folder);
    formData.append("fileName", file.name);
    formData.append("mimeType", file.type || "application/octet-stream");
    if (cardId) formData.append("cardId", cardId);

    return new Promise<DriveUploadResult>((resolve, reject) => {
      const xhr = new XMLHttpRequest();
      xhr.open("POST", "/api/drive/proxy-upload", true);

      xhr.upload.onprogress = (e) => {
        if (e.lengthComputable) {
          onProgress?.(Math.round((e.loaded / e.total) * 90));
        }
      };

      xhr.onload = () => {
        onProgress?.(95);
        if (xhr.status >= 200 && xhr.status < 300) {
          try {
            const data = JSON.parse(xhr.responseText);
            if (data.error) {
              reject(new Error(data.error));
            } else {
              onProgress?.(100);
              resolve({
                fileId: data.fileId,
                url: data.url,
                mimeType: data.mimeType,
                size: data.size,
              });
            }
          } catch {
            reject(new Error("Invalid response from upload server"));
          }
        } else {
          try {
            const data = JSON.parse(xhr.responseText);
            reject(new Error(data.error || `Upload failed: ${xhr.status}`));
          } catch {
            reject(new Error(`Upload failed: ${xhr.status}`));
          }
        }
      };

      xhr.onerror = () => reject(new Error("Network error during upload"));
      xhr.ontimeout = () => reject(new Error("Upload timed out"));
      xhr.send(formData);
    });
  }, "Proxy upload");
}

// ─── Resumable path (>= 4 MB) ─────────────────────────────────────────────

async function getUploadSession(
  file: File,
  folder: string,
  cardId: string | undefined
): Promise<string> {
  const res = await fetch("/api/drive/upload", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      fileName: file.name,
      mimeType: file.type || "application/octet-stream",
      folder,
      cardId,
      fileSize: file.size,
    }),
  });

  if (!res.ok) {
    let msg = `Session creation failed: ${res.status}`;
    try {
      const data = await res.json();
      if (data.error) msg = data.error;
    } catch { /* ignore */ }
    throw new Error(msg);
  }

  const data = await res.json();
  if (!data.uploadUri) throw new Error("No uploadUri returned from session endpoint");
  return data.uploadUri;
}

async function putToGoogle(
  file: File,
  uploadUri: string,
  onProgress: ProgressCallback | undefined
): Promise<string> {
  return new Promise<string>((resolve, reject) => {
    const xhr = new XMLHttpRequest();
    xhr.open("PUT", uploadUri, true);
    xhr.setRequestHeader("Content-Type", file.type || "application/octet-stream");

    xhr.upload.onprogress = (e) => {
      if (e.lengthComputable) {
        // Map to 5–90% of overall progress
        onProgress?.(5 + Math.round((e.loaded / e.total) * 85));
      }
    };

    xhr.onload = () => {
      if (xhr.status === 200 || xhr.status === 201) {
        try {
          const data = JSON.parse(xhr.responseText);
          if (!data.id) {
            reject(new Error("Google did not return a file ID after upload"));
          } else {
            resolve(data.id as string);
          }
        } catch {
          reject(new Error("Invalid response from Google after upload"));
        }
      } else {
        reject(new Error(`Upload to Google failed: ${xhr.status}`));
      }
    };

    xhr.onerror = () => reject(new Error("Network error during upload to Google"));
    xhr.ontimeout = () => reject(new Error("Upload to Google timed out"));
    xhr.send(file);
  });
}

async function finalizeUpload(fileId: string): Promise<DriveUploadResult> {
  const res = await fetch("/api/drive/finalize", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ fileId }),
  });

  if (!res.ok) {
    let msg = `Finalize failed: ${res.status}`;
    try {
      const data = await res.json();
      if (data.error) msg = data.error;
    } catch { /* ignore */ }
    throw new Error(msg);
  }

  const data = await res.json();
  return {
    fileId: data.fileId,
    url: data.url,
    mimeType: data.mimeType,
    size: data.size,
  };
}

async function uploadViaResumable(
  file: File,
  folder: string,
  cardId: string | undefined,
  onProgress: ProgressCallback | undefined
): Promise<DriveUploadResult> {
  onProgress?.(0);

  // Step 1: create resumable session (returns uploadUri)
  const uploadUri = await withRetry(
    () => getUploadSession(file, folder, cardId),
    "Session creation"
  );
  onProgress?.(5);

  // Step 2: PUT file directly to Google (bypasses Vercel)
  const fileId = await putToGoogle(file, uploadUri, onProgress);
  onProgress?.(90);

  // Step 3: set permissions and get serving URL
  const result = await withRetry(
    () => finalizeUpload(fileId),
    "Finalize"
  );
  onProgress?.(100);

  return result;
}

// ─── Public API ────────────────────────────────────────────────────────────

export async function uploadToDrive(
  file: File,
  folder: "thumbnails" | "raw-files" | "media-library",
  cardId?: string,
  onProgress?: ProgressCallback
): Promise<DriveUploadResult> {
  if (file.size === 0) throw new Error("Cannot upload empty file");
  if (file.size > MAX_FILE_SIZE) throw new Error(`File exceeds ${MAX_FILE_SIZE / (1024 * 1024)}MB limit.`);

  onProgress?.(0);

  if (file.size >= RESUMABLE_THRESHOLD) {
    return uploadViaResumable(file, folder, cardId, onProgress);
  }
  return uploadViaProxy(file, folder, cardId, onProgress);
}
---
END OF FILE CONTENT FOR src/lib/drive-upload.ts


================================================================================
FIX 2 OF 3: Revision Email Not Sent When Dragging Card to "Revision Needed"
================================================================================

WHY: There are two ways to request a revision:
  A) Drag a card to the "Revision Needed" column on the Kanban board — opens the
     KickbackModal. The user fills in feedback and submits.
  B) Click "Request Revision" in the asset review drawer.

Path B calls /api/notifications/revision and sends email. Path A (the modal)
calls `submitKickback()` in pipeline-context.tsx which only updates the card and
writes an audit log. No email is sent. The post creator has no idea their work
was sent back unless they check the board.

Also: the KickbackModal uses MentionTextarea (supports @Name tags), but the @
mentions are never turned into email notifications.

THE FIX: Add notification calls to `submitKickback()` in pipeline-context.tsx.
Also update the KickbackModal toast message to reflect that notifications are sent.

NOTE ON USER EMAIL: This codebase uses `userName` (a name string prop) rather
than a user email. The `requestedBy` field will contain the user's display name
instead of their email. This means the "exclude requester" logic in the revision
API won't exclude them from receiving the email (name != email address comparison).
For a small team this is acceptable behavior — worst case the approver also gets
the notification email they just sent.

--- CHANGE 2A: src/lib/pipeline-context.tsx ---

Find the `submitKickback` function (around line 299). It currently ends with:

    setPendingKickback(null);
    logAudit(cardId, author, "revision_requested", `Kickback: ${note}`);
  }, [useSupabase, cards, userName]);

Replace the entire `submitKickback` function with the version below. The key
changes are:
1. Card is captured BEFORE setCards (so we have title + createdBy for the notification)
2. Notification calls added after audit log
3. `userName` added to dependency array (was already there, keeping it)

FIND the entire submitKickback function:
  const submitKickback = useCallback((cardId: string, note: string, attachmentUrl?: string) => {
    const now = new Date();
    const timestamp = now.toLocaleString("en-US", { timeZone: "America/Chicago", month: "short", day: "numeric", hour: "numeric", minute: "2-digit" });
    const author = userName;
    let noteLine = `${author} (${timestamp}): Revision requested — ${note}`;
    if (attachmentUrl) noteLine += `\n📎 ${attachmentUrl}`;

    setCards((prev) => prev.map((c) => {
      if (c.id !== cardId) return c;
      const notes = c.notes ? c.notes + "\n\n" + noteLine : noteLine;
      return { ...c, stage: "revision_needed" as PipelineStage, revised: false, notes, updatedAt: now.toISOString() };
    }));
    setSelectedCard((prev) => {
      if (prev?.id !== cardId) return prev;
      const notes = prev.notes ? prev.notes + "\n\n" + noteLine : noteLine;
      return { ...prev, stage: "revision_needed" as PipelineStage, revised: false, notes };
    });

    if (useSupabase) {
      markMutation(cardId);
      const card = cards.find((c) => c.id === cardId);
      const notes = card?.notes ? card.notes + "\n\n" + noteLine : noteLine;
      supabase.from("posts").update({ stage: "revision_needed", notes }).eq("id", cardId).then(() => {});
    }

    setPendingKickback(null);
    logAudit(cardId, author, "revision_requested", `Kickback: ${note}`);
  }, [useSupabase, cards, userName]);

REPLACE WITH:
  const submitKickback = useCallback((cardId: string, note: string, attachmentUrl?: string) => {
    const now = new Date();
    const timestamp = now.toLocaleString("en-US", { timeZone: "America/Chicago", month: "short", day: "numeric", hour: "numeric", minute: "2-digit" });
    const author = userName;
    // Capture card before state mutation so notification can read title + createdBy
    const card = cards.find((c) => c.id === cardId);
    let noteLine = `${author} (${timestamp}): Revision requested — ${note}`;
    if (attachmentUrl) noteLine += `\n📎 ${attachmentUrl}`;

    setCards((prev) => prev.map((c) => {
      if (c.id !== cardId) return c;
      const notes = c.notes ? c.notes + "\n\n" + noteLine : noteLine;
      return { ...c, stage: "revision_needed" as PipelineStage, revised: false, notes, updatedAt: now.toISOString() };
    }));
    setSelectedCard((prev) => {
      if (prev?.id !== cardId) return prev;
      const notes = prev.notes ? prev.notes + "\n\n" + noteLine : noteLine;
      return { ...prev, stage: "revision_needed" as PipelineStage, revised: false, notes };
    });

    if (useSupabase) {
      markMutation(cardId);
      const notes = card?.notes ? card.notes + "\n\n" + noteLine : noteLine;
      supabase.from("posts").update({ stage: "revision_needed", notes }).eq("id", cardId).then(() => {});
    }

    setPendingKickback(null);
    logAudit(cardId, author, "revision_requested", `Kickback: ${note}`);

    // Notify creator + all Creative Directors
    fetch("/api/notifications/revision", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        postId: cardId,
        postTitle: card?.title ?? "",
        revisionNote: note,
        requestedBy: userName,
        createdBy: card?.createdBy,
      }),
    }).catch(() => {});

    // Fire @mention notifications if anyone was tagged
    if (note.includes("@")) {
      fetch("/api/notifications/mention", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          comment: note,
          postTitle: card?.title ?? "",
          postId: cardId,
          authorName: userName,
          authorEmail: userName,
        }),
      }).catch(() => {});
    }
  }, [useSupabase, cards, userName]);

--- CHANGE 2B: src/components/kickback-modal.tsx ---

Update the toast message to reflect that the creator is now notified.

FIND:
    addToast("Revision requested — card moved back for fixes.", "warning");

REPLACE WITH:
    addToast("Revision requested — creator and Creative Director notified.", "warning");


================================================================================
FIX 3 OF 3: Revision Email Template Unbranded (Dark Theme, No Logo)
================================================================================

WHY: The revision email in /api/notifications/revision/route.ts uses inline HTML
with a dark background (#0a0a0e), no logo, and is inconsistent with every other
email in the system. All other emails use the `emailShell()` wrapper with the
Prime Time logo, navy theme, and blue accents.

Also: the email subject uses raw string interpolation (header injection risk).
A `safeSubject()` utility needs to be added.

THE FIX:
1. Add `safeSubject()` export to email-utils.ts (strips CR/LF from subjects)
2. Make the `esc()` function exported (currently private)
3. Add `buildRevisionEmailHtml()` to email-utils.ts using the `emailShell()` pattern
4. Update the revision route to use the new template and `safeSubject()`

--- CHANGE 3A: src/lib/email-utils.ts ---

FIND the private `esc` function:
  function esc(str: string): string {
    return str.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
  }

REPLACE WITH (make it exported and handle null/undefined safely):
  export function esc(value: unknown): string {
    if (value == null) return "";
    return String(value)
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;")
      .replace(/'/g, "&#39;");
  }

Then ADD the following two functions immediately after the `esc` function
(before `getTransporter`):

  /** Strip CR/LF from email subject lines to prevent header injection. */
  export function safeSubject(value: unknown): string {
    if (value == null) return "";
    return String(value).replace(/[\r\n]+/g, " ").trim().slice(0, 200);
  }

Then ADD the following new template function at the END of the file (after the
`buildApprovalEmailHtml` function):

  // ─── Revision Requested Email ───

  export function buildRevisionEmailHtml(postTitle: string, revisionNote: string, requestedBy: string, siteUrl: string): string {
    return emailShell(
      "#dc2626",
      "Revision Requested",
      "Your post has been sent back for fixes",
      `
      <p style="color:#374151; font-size:14px; line-height:1.7; margin:0 0 8px;">
        <strong style="color:#111827;">${esc(requestedBy)}</strong> has requested revisions on:
      </p>
      <p style="color:#111827; font-size:17px; font-weight:700; margin:0 0 20px; line-height:1.3;">${esc(postTitle)}</p>
      <table width="100%" cellpadding="0" cellspacing="0" style="margin-bottom:24px;">
        <tr><td style="background:#fff5f5; border-left:4px solid #dc2626; border-radius:0 8px 8px 0; padding:14px 18px;">
          <p style="color:#6b7280; font-size:10px; font-weight:700; text-transform:uppercase; letter-spacing:0.1em; margin:0 0 6px;">What needs to be fixed</p>
          <p style="color:#374151; font-size:14px; line-height:1.6; margin:0; white-space:pre-wrap;">${esc(revisionNote)}</p>
        </td></tr>
      </table>
      ${ctaButton(siteUrl, "View in Content Engine")}
      <p style="color:#9ca3af; font-size:11px; text-align:center; margin:20px 0 0;">Log in to make your changes and resubmit for approval.</p>
      `
    );
  }

--- CHANGE 3B: src/app/api/notifications/revision/route.ts ---

Update the import line at the top to add `safeSubject` and `buildRevisionEmailHtml`:

FIND:
  import { getTransporter, getFromAddress } from "@/lib/email-utils";

REPLACE WITH:
  import { getTransporter, getFromAddress, safeSubject, buildRevisionEmailHtml } from "@/lib/email-utils";

Then replace the entire inline HTML block with a single function call.

FIND (the entire htmlEmail variable assignment — the dark-themed inline HTML):
      const htmlEmail = `
        <div style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; max-width: 540px; margin: 0 auto; background: #0a0a0e; border-radius: 16px; overflow: hidden;">
          <div style="padding: 32px 28px 24px; border-bottom: 1px solid rgba(255,255,255,0.06);">
            <div style="display: flex; align-items: center; gap: 12px; margin-bottom: 20px;">
              <div style="width: 40px; height: 40px; border-radius: 10px; background: linear-gradient(135deg, #0056b3, #1e40af); display: flex; align-items: center; justify-content: center;">
                <span style="color: white; font-size: 18px; font-weight: 800;">!</span>
              </div>
              <div>
                <p style="color: #0056b3; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; margin: 0;">Revision Requested</p>
                <p style="color: #6b7280; font-size: 12px; margin: 2px 0 0;">by ${body.requestedBy}</p>
              </div>
            </div>
            <h2 style="color: #ffffff; font-size: 20px; font-weight: 700; margin: 0 0 16px; letter-spacing: -0.02em;">${body.postTitle}</h2>
            <div style="background: rgba(0,86,179,0.08); border-left: 3px solid #0056b3; padding: 14px 18px; border-radius: 0 10px 10px 0;">
              <p style="color: #93c5fd; font-size: 13px; line-height: 1.6; margin: 0; white-space: pre-wrap;">${body.revisionNote}</p>
            </div>
          </div>
          <div style="padding: 24px 28px;">
            <a href="${siteUrl}" style="display: inline-block; background: #0056b3; color: white; text-decoration: none; padding: 12px 28px; border-radius: 10px; font-size: 13px; font-weight: 700; letter-spacing: 0.01em;">
              View Revision in Portal
            </a>
            <p style="color: #374151; font-size: 11px; margin: 16px 0 0;">Prime Time Window Cleaning &mdash; Content Pipeline</p>
          </div>
        </div>
      `;

REPLACE WITH:
      const htmlEmail = buildRevisionEmailHtml(body.postTitle, body.revisionNote, body.requestedBy, siteUrl);

Also update the sendMail call to use `safeSubject()` for header injection safety.

FIND:
            subject: `Revision Requested: "${body.postTitle}"`,

REPLACE WITH:
            subject: safeSubject(`Revision Requested: "${body.postTitle}"`),


================================================================================
VERIFICATION
================================================================================

1. Run: npm run build
   Expected: Clean compile, zero TypeScript errors, all routes listed.

2. Start dev server: npm run dev

3. TEST — Video upload (Fix 1):
   - Open Create Post modal
   - Attach a small image (< 4 MB) → should upload via proxy path, works as before
   - Attach an MP4 video (> 4.5 MB) → should use resumable path, NO 413 error
   - Check the Google Drive folder — file should appear with timestamp name

4. TEST — Revision notification (Fix 2):
   - Drag any card to the "Revision Needed" column
   - KickbackModal opens — fill in at least 10 characters of feedback
   - Click "Request Revision"
   - Toast should say "Revision requested — creator and Creative Director notified."
   - Card moves to Revision Needed column
   - Post creator should receive a branded email notification

5. TEST — Email branding (Fix 3):
   - Trigger a revision from either path (drag or asset-review-drawer)
   - Open the received email
   - Should have: Prime Time logo, navy/blue theme, red accent bar for the
     revision section, proper footer with "Prime Time Window Cleaning"
   - Should NOT be the old dark (#0a0a0e) themed email

6. Build check: npm run build (repeat after all changes to confirm no regressions)

================================================================================
END OF MIGRATION PROMPT
================================================================================
